<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="projetoAndroid.Pages.periodos"
             Title="periodos">

    <ScrollView>
        <VerticalStackLayout Padding="20"
                             Spacing="20">

            <Entry
                x:Name="etrPeriodos"
                Placeholder="Pesquise aqui"/>

                <Label 
                x:Name="lblPeriodos"
                Text="Períodos"
                FontAttributes="Bold"
                FontSize="Title"/>

            <BoxView
                x:Name="bxvGuardandoCaixao"
                HeightRequest="200"
                Color="LightGray"/>

            <Button
                x:Name="btnAddPeriodo"
                Text="+ ADICIONAR"
                Clicked="OnClickPaginaAdicionarPeriodo"/>

            <Button
                x:Name="btnExcluirPeriodo"
                Text="EXCLUIR"/>

            <Button
                x:Name="btnModificarPeriodo"
                Text="EDITAR"
                Clicked="OnClickPaginaEditarPeriodo"/>

        </VerticalStackLayout>
    </ScrollView>
</ContentPage>